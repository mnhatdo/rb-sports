# MongoDB Connection String
# Để kết nối MongoDB Atlas, thêm connection string vào đây:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/rb-sports?retryWrites=true&w=majority

# Nếu không có, server sẽ dùng in-memory storage (data mất khi restart)
